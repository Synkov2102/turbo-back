# Быстрый старт деплоя

## 🚀 Что было настроено

1. ✅ **Dockerfile** - production-ready образ с поддержкой Puppeteer
2. ✅ **GitHub Actions CI/CD** - автоматическая сборка и деплой
3. ✅ **Docker Compose** - для локального запуска и продакшн
4. ✅ **Переменные окружения** - поддержка CORS и других настроек через env

## 📋 Следующие шаги

### 1. Настройте GitHub Secrets (для автоматического деплоя)

Перейдите в `Settings > Secrets and variables > Actions` и добавьте:

```
VPS_SSH_HOST=your-server.com
VPS_SSH_USER=deploy
VPS_SSH_KEY_B64=<ваш приватный SSH ключ в base64>
VPS_DEPLOY_PATH=/opt/turbo-back  # опционально
```

**Как получить base64 ключ:**
```bash
# Linux/WSL
cat ~/.ssh/id_rsa | base64 -w 0

# macOS
cat ~/.ssh/id_rsa | base64

# Windows PowerShell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("$env:USERPROFILE\.ssh\id_rsa"))
```

Скопируйте весь вывод (одну длинную строку) и вставьте в `VPS_SSH_KEY_B64`.

### 2. Первый деплой

```bash
# На сервере
mkdir -p /opt/turbo-back
cd /opt/turbo-back

# Скопируйте файлы:
# - docker-compose.prod.yml
# - .env (создайте на основе env.example)

# Запустите
docker-compose -f docker-compose.prod.yml up -d
```

### 3. Проверка

```bash
# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f app

# Проверьте API
curl http://localhost:3001/api
```

## 📚 Дополнительная документация

- Полное руководство: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)
- Настройка прокси: [docs/PROXY_SETUP.md](docs/PROXY_SETUP.md)

## 🔧 Локальная разработка

```bash
# Сборка образа
docker build -t turbo-back .

# Запуск с docker-compose (включая MongoDB)
docker-compose up -d
```

## ⚙️ Переменные окружения

Скопируйте `env.example` в `.env` и настройте:

- `MONGO_URI` - подключение к MongoDB
- `CORS_ORIGINS` - разрешенные домены для CORS
- `PORT` - внешний порт на хосте (по умолчанию 3002, внутри контейнера всегда 3001)

